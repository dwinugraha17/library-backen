{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'chmod -R 777 storage bootstrap/cache && php artisan storage:link && php artisan config:clear && php artisan cache:clear && php -S 0.0.0.0:$PORT -t public router.php'",
    "runtime": "V2",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
